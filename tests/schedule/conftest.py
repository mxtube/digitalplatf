import pytest
from schedule.models import Schedule
from datetime import date


# @pytest.fixture
# def schedule(studygroup):
#     """Create a sample schedule fixture for the department."""
#     return Schedule.objects.create(date=date.today(), group=department.groups.first())
